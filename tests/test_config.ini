[DEFAULT]
debug = true
## Note: these are Ian's settings:
smtp_password_filename = ~/.smtp-password
smtp_username = ianbicking
smtp_server = mail.speakeasy.net

[app:main]
use = egg:EmailIt

[server:main]
use = egg:Paste#http
port = 8080
